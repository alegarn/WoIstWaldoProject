# Reference values for Maestro flow env vars. NOT loaded via --env-file.
# Pass individually via -e KEY=VALUE on the CLI (Maestro 2.3.0).
#
# EXPO_PUBLIC_E2E_MODE=true must be present when you start Expo / the dev client
# or when you build the app under test. This file does not toggle the app runtime mode.
LOGIN_EMAIL: a@a.com
LOGIN_PASSWORD: aaaaaa
# Signup credentials are generated at runtime by .maestro/scripts/generate-signup-credentials.js.
SIGNUP_EMAIL_PREFIX: e2e.signup
SIGNUP_EMAIL_DOMAIN: woistwaldo.test
SIGNUP_PASSWORD: E2eSignup123!
SIGNUP_USERNAME_PREFIX: e2e_signup
HIDE_DESCRIPTION: Hidden near the left side of the tree.
RESULT_SUCCESS_TEXT: You Found It!
RANKING_ROW_1_NAME: John